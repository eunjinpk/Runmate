<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Runmate">

        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.SignupActivity"
            android:exported="false" />

        <activity
            android:name=".ui.place.ParkSelectActivity"
            android:exported="false" />

        <activity
            android:name=".ui.place.CourseListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.meeting.CreateMeetingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.meeting.MeetingDetailActivity"
            android:exported="false" />

        <activity
            android:name=".MyPageActivity"
            android:exported="false" />

        <activity
            android:name=".ProfileSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".CustomerServiceActivity"
            android:exported="false" />

    </application>

</manifest>